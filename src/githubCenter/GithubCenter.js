import React from 'react';
import GithubCenter from "styled-components";
import img from "../githubImage/co-authoring.png";
import

const GithubCenter = ({ chn, text, img }) => {
    return(
        <Container>
            <Wrapper chn={chn}>

            </Wrapper>
        </Container>
    )
}
)

export const GithubCenter = () => {
    return (
        <div>
            
        </div>
    )
}
